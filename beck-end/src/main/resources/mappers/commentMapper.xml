<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.instagram.comment.model.mapper.CommentMapper">
<!-- 특정 게시물 댓글 목록 조회 -->
    <select id="selectCommentsByPostId" resultType="Comment">
        SELECT *
        FROM comment_detail_view
        WHERE post_id = #{postId}
        ORDER BY created_at
    </select>

<!-- 댓글 작성 -->
    <insert id="inserComment" parameterType="Comment" useGeneratedKeys="true" keyProperty="commentId" keyColumn="comment_id">
        INSERT INTO comments(post_id, user_id, comment_content)
        VALUES(#{postId}, #{userId}, #{commentContent})
    </insert>
<!-- 댓글 삭제 -->
    <delete id="deleteComment">
        DELETE FROM comments
        WHERE comment_id = #{commentId}
    </delete>
<!-- 댓글 수정 -->
    <update id="updateComment">
        UPDATE comments
        SET comment_content = ${commentContent},
            updated_at = current_timestamp
        WHERE comment_id = #{commentId}
    </update>

</mapper>